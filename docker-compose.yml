version: '3.7'

services:

  counter_app:
    container_name: ShoppingCar
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/usr/app'
      - '/usr/app/node_modules'
    ports:
      - 4000:4000